% !TeX root = ../AIMALPHA_documentation.tex
This section explains the calibration and processing of data used in the model.  
Exogenous variables and parameters were estimated using multiple datasets, adjusted to 
obtain equilibrium in the base year, and parameters were estimated by regression analysis.

\bigskip

\subsection{Data Sources}
Table~\ref{tab:data_sources} lists the datasets used in the model.

\clearpage

\begin{xltabular}{\linewidth}{l X X}
  \caption{List of data sources}\label{tab:data_sources}\\
  \toprule
  \textbf{Data Source} & \textbf{Dataset Name} & \textbf{URL} \\
  \midrule
  \endfirsthead

  \caption[]{List of data sources (continued)}\\
  \toprule
  \textbf{Data Source} & \textbf{Dataset Name} & \textbf{URL} \\
  \midrule
  \endhead

  \midrule
  \multicolumn{3}{r}{\footnotesize Continued on next page} \\
  \endfoot

  \bottomrule
  \endlastfoot

  FAOSTAT~\cite{FAOSTATa} & Food Balances   & \nolinkurl{https://www.fao.org/faostat/en/\#data/FBS} \\
  FAOSTAT~\cite{FAOSTATa} & Production      & \nolinkurl{https://www.fao.org/faostat/en/\#data/QCL} \\
  FAOSTAT~\cite{FAOSTATa} & Trade           & \nolinkurl{https://www.fao.org/faostat/en/\#data/TCL} \\
  FAOSTAT~\cite{FAOSTATa} & Producer Prices & \nolinkurl{https://www.fao.org/faostat/en/\#data/PP} \\
  FAOSTAT~\cite{FAOSTATa} & Land Use        & \nolinkurl{https://www.fao.org/faostat/en/\#data/RL} \\
  FAOSTAT~\cite{FAOSTATa} & Emissions       & \nolinkurl{https://www.fao.org/faostat/en/\#data/GCE} \\
  FAO Land and Water Division~\cite{faolandandwaterdivisionMajorHydrologicalBasins2011} & Major Hydrological Basins of the World & \nolinkurl{https://data.apps.fao.org/catalog//iso/7707086d-af3c-41cc-8aa5-323d8609b2d1} \\
  FAO and IIASA~\cite{faoGlobalAgroEcologicalZones2025} & GAEZ v4   & \nolinkurl{https://gaez.fao.org/} \\
  IIASA & SSP 3.0          & \nolinkurl{https://data.ece.iiasa.ac.at/ssp/\#/about} \\
  GTAP~\cite{aguiarGTAPDataBase2019}  & GTAP 10          & \nolinkurl{https://jgea.org/ojs/index.php/jgea/article/view/77} \\
  IFPRI~\cite{internationalfoodpolicyresearchinstituteifpriGlobalSpatiallyDisaggregatedCrop2024} & MAPSPAM          & \nolinkurl{https://doi.org/10.7910/DVN/SWPENT} \\
  IFPRI~\cite{robinsonInternationalModelPolicy2015} & IMPACT          & \nolinkurl{https://github.com/IFPRI/IMPACT} \\
  World Bank & World Bank Open Data & \nolinkurl{https://data.worldbank.org/} \\
  EDGAR~\cite{europeancommissionjointresearchcentrejrcEDGAREmissionsDatabase2024} & EDGAR v8.0       & \nolinkurl{https://edgar.jrc.ec.europa.eu/dataset_ghg80} \\
  Herrero et~al.~(2018)~\cite{herreroLivestockProductionSystems2018} & Livestock production systems & \nolinkurl{https://doi.org/10.4225/08/5aa068b33fe06} \\
  Muhammad et~al.~(2011)~\cite{muhammadInternationalEvidenceFood2013}   & International Evidence on Food Consumption Patterns & \nolinkurl{http://199.135.94.241/publications/pub-details/?pubid=47581} \\
  T.~P.~Robinson et~al.~(2018)~\cite{robinsonGlobalDistributionRuminant2018} & Global distribution of ruminant livestock production systems V5 & \nolinkurl{https://doi.org/10.7910/DVN/WPDSZE} \\
  Harmsen et~al.~(2019)~\cite{harmsenLongtermMarginalAbatement2019a}    & CH\textsubscript{4} and N\textsubscript{2}O MAC curves & \nolinkurl{https://doi.org/10.1016/j.envsci.2019.05.013} \\
  Hurtt et~al.~(2019)~\cite{hurttHarmonizationGlobalLand2020}      & Land Use Harmonization 2 & \nolinkurl{https://luh.umd.edu/index.shtml} \\
  Hanasaki et~al.~(2008)~\cite{hanasakiIntegratedModelAssessment2008a,hanasakiIntegratedModelAssessment2008b} & H08 rainfed crop yield & \nolinkurl{https://doi.org/10.5194/hess-12-1007-2008} \\
  Hasegawa et~al.~(2017)~\cite{hasegawaGlobalLanduseAllocation2017} & AIM-PLUM spatial land-use outputs & \nolinkurl{https://doi.org/10.1016/j.scitotenv.2016.12.025} \\
  Ito and Inatomi (2012)~\cite{itoWaterUseEfficiencyTerrestrial2012} & VISIT carbon-cycle outputs & \nolinkurl{https://doi.org/10.1175/JHM-D-10-05034.1} \\
  Gao and Pesaresi (2021)~\cite{gaoGlobal1kmDownscaled2021} & Global 1-km Downscaled Urban Land Extent Projection and Base Year Grids by SSP Scenarios, 2000-2100 & \nolinkurl{https://doi.org/10.7927/1z4r-ez63} \\
  Gao and Pesaresi (2021)~\cite{gaoDownscalingSSPconsistent2021} & Downscaling SSP-consistent Global Spatial Urban Land Projections from 1/8-degree to 1-km Resolution, 2000-2100 & \nolinkurl{https://doi.org/10.1038/s41597-021-01052-0} \\
  IUCN and UNEP-WCMC~\cite{iucnUNEPWCMCWorldDatabase2025} & World Database on Protected Areas (WDPA), Dec 2025 release & \nolinkurl{https://www.protectedplanet.net} \\
\end{xltabular}

\subsection{Calibration and processing of data}
\subsubsection{Adjustment of demand-side data}

First, the adjustment of demand-side data was conducted. 
In the model, if a country–commodity pair has missing data for the base year 2015, it is assumed that consumption does not occur thereafter. 
Therefore, for country–commodity pairs without 2015 data in the Food Balance Sheets (FBS), the average value of subsequent years was used as the 2015 consumption level.

\begin{equation}
qdFood_{(c,cty,2015)} = \frac{\sum_{yr} qdFood^{FAO}_{(c,cty,yr)}}{N_{yr}}
\quad \text{for } qdFood_{(c,cty,2015)} = \text{NA}
\end{equation}

where \( qdFood^{FAO} \) is household consumption data from FAO,
and \( N_{yr} \) is the number of years for which data exist.

In the FBS data, household consumption includes the amount of food wasted at the household level. 
To distinguish this, waste rates by seven commodities and seven regions were obtained from FAO (2011)~\cite{faoGlobalFoodLossesAndFoodWaste2011}. 
Using these coefficients, household consumption and food waste were estimated as follows:

\begin{align}
qdWaste_{(c,cty)} &= qdFood^{FAO}_{(c,cty)} \cdot wasteRate_{(c,cty)} \\
qdFood_{(c,cty)} &= qdFood^{FAO}_{(c,cty)} \cdot (1 - wasteRate_{(c,cty)})
\end{align}

For model simplification, the commodities used as livestock feed, processing materials, and biofuel inputs were limited. 
Wheat, rice, maize, other cereals, roots and tubers, sugar crops, pulses, oil crops, and vegetables were used as livestock feed. 
Wheat, rice, maize, other cereals, and fruits were used as raw materials for alcoholic beverages. 
Sugar crops were used as inputs for sugars, oil crops for vegetable oils, and maize, sugar crops, and vegetable oils for biofuels. 
Feed demand, intermediate input demand, and biofuel demand for other commodities were set to zero.

\begin{align}
qdFed_{(c \notin c_{fed},cty)} &= 0 \\
qdBio_{(c \notin c_{bio},cty)} &= 0 \\
qdInt_{(c \notin c_{int},cty)} &= 0
\end{align}

As a result of these adjustments, the total domestic demand changed. 
If the sum of all demand components was less than the total demand from the data, 
the residual was added as “other demand”:

\begin{align}
qdOth_{(c,cty)} &= qd_{(c,cty)} -
  \nonumber\\[-0.3em]
  &\quad
  \big(qdFood_{(c,cty)} + qdFed_{(c,cty)} + qdInt_{(c,cty)} + qdSeed_{(c,cty)} +
  \nonumber\\[-0.3em]
  &\quad
  qdTour_{(c,cty)} + qdLoss_{(c,cty)} + qdWaste_{(c,cty)}\big)
\end{align}

\subsubsection{Adjustment of crop production data}
Next, the adjustment of crop production data was conducted. 
From the FBS data, the production quantities of primary crops for each country were obtained. 
These were disaggregated into production units and production technologies using the GAEZ data as follows:

\begin{equation}
qsWat_{(a\_cro,bas,cty,wat)} =
qs^{FAO}_{(a\_cro,cty)} \cdot
\frac{qsGaez^{GAEZ}_{(a\_cro,bas,cty,wat)}}{\sum_{(bas,wat)} qsGaez^{GAEZ}_{(a\_cro,bas,cty,wat)}}
\end{equation}

where \( qs^{FAO} \) denotes production quantity data from FAO,
and \( qsGaez^{GAEZ} \) represents production data from GAEZ.

From the LUH2 dataset, the cultivated land area for each production unit was obtained. 
Using the FAOSTAT data on national land use, these cultivated lands were divided into harvested and fallow lands as follows:

\begin{align}
qlCroCul_{(bas,cty)} &=
qlCro^{LUH2}_{(bas,cty)} \cdot
\frac{qlCroCul^{FAO}_{cty}}{qlCroCul^{FAO}_{cty} + qlCroFal^{FAO}_{cty}} \\
qlCroFal_{(bas,cty)} &=
qlCro^{LUH2}_{(bas,cty)} \cdot
\frac{qlCroFal^{FAO}_{cty}}{qlCroCul^{FAO}_{cty} + qlCroFal^{FAO}_{cty}}
\end{align}

where \( qlCro^{LUH2} \) denotes cultivated area data from LUH2,
\( qlCroCul^{FAO} \) is harvested land area data from FAO,
and \( qlCroFal^{FAO} \) is fallow land area data from FAO.

Furthermore, the harvested land was disaggregated by crop type and production technology 
using GAEZ data as follows:

\begin{equation}
qlWat_{(a\_cro,bas,cty,wat)} =
qlCroCul_{(bas,cty)} \cdot
\frac{qlGaez^{GAEZ}_{(a\_cro,bas,cty,wat)}}{\sum_{(a\_cro,wat)} qlGaez^{GAEZ}_{(a\_cro,bas,cty,wat)}}
\end{equation}

where \( qlWat \) is the cropland area by crop type and production technology,
and \( qlGaez^{GAEZ} \) is the crop-specific land area from GAEZ.

From the MAPSPAM dataset, harvested and physical area data were obtained for each crop, 
production unit, and production technology. 
Using these data, the cropping intensity was estimated as follows:

\begin{equation}
croInt_{(a\_cro,bas,cty,wat)} =
\frac{mapspam^{Harvested}_{(a\_cro,bas,cty,wat)}}{mapspam^{Physical}_{(a\_cro,bas,cty,wat)}}
\end{equation}

where \( mapspam^{Harvested} \) is the harvested area data from MAPSPAM, and \\
\( mapspam^{Physical} \) is the physical area data from MAPSPAM.

Crop yields were calculated using the obtained production quantities, harvested areas, 
and cropping intensities as follows:

\begin{equation}
yld_{(a\_cro,bas,cty,wat)} =
\frac{qsWat_{(a\_cro,bas,cty,wat)}}{qlWat_{(a\_cro,bas,cty,wat)} \cdot croInt_{(a\_cro,bas,cty,wat)}}
\end{equation}

Food-crop yields are based on FAOSTAT production and GAEZ spatial allocation. For dedicated energy crops, AIM-ALPHA instead uses the rainfed yield from H08, following the bioenergy-potential assessment of Wu et~al.~\cite{hanasakiIntegratedModelAssessment2008a,hanasakiIntegratedModelAssessment2008b,wuGlobalAdvancedBioenergy2019}.

When feedback from AIM-PLUM is enabled, the crop-yield driver is adjusted by the change in basin-average yield associated with changes in the grid-level spatial allocation of crop production~\cite{hasegawaGlobalLanduseAllocation2017}. The adjustment is one when this feedback is not used.


\subsubsection{Adjustment of livestock production data}

Next, the adjustment of livestock production data was conducted. 
From FAO data, the numbers of livestock animals and slaughtered (or milked) animals were obtained. 
Using these, the production rate was calculated. 
A minimum threshold of 0.3 was set for the production rate; if it fell below this value, 
it was assumed that a portion of livestock was not involved in production activities.

\begin{align}
animalProdRate_{(a\_liv,cty)} &=
\frac{nAnimal^{FAO}_{(a\_liv,cty)}}{nAnimalStock^{FAO}_{(a\_liv,cty)}} \\
nAnimalRes_{(a\_liv,cty)} &=
nAnimalStock^{FAO}_{(a\_liv,cty)} -
\frac{nAnimal^{FAO}_{(a\_liv,cty)}}{0.3}
\nonumber\\[-0.3em]
&\quad
\quad (\text{if } animalProdRate_{(a\_liv,cty)} < 0.3)
\end{align}

where \( nAnimal^{FAO} \) is the number of slaughtered (or milked) animals from FAO,
and \( nAnimalStock^{FAO} \) is the total livestock population.

When non-productive livestock exist, the corresponding unused pasture area was estimated as:

\begin{equation}
qlPasRes_{(a\_liv,cty)} =
qlPas^{LUH2}_{(a\_liv,cty)} \cdot
\frac{nAnimalRes_{(a\_liv,cty)}}{nAnimalStock^{FAO}_{(a\_liv,cty)}}
\end{equation}

where \( qlPasRes \) denotes the unused pasture area,
and \( qlPas^{LUH2} \) is the pasture area from LUH2.

Country-level livestock production data from the FBS were disaggregated 
into production units, climate zones, and production systems 
using the dataset from Herrero et al. (2018). 
Similarly, LUH2 pasture area data were disaggregated by livestock type, 
climate zone, and production system.

\begin{align}
qsSys_{(a\_liv,bas,cty,cz,sys)} &=
qs^{FAO}_{(a\_liv,cty)} \cdot
\frac{livWt^{HERRERO}_{(a\_liv,bas,cty,cz,sys)}}
{\sum_{(bas,cz,sys)} livWt^{HERRERO}_{(a\_liv,bas,cty,cz,sys)}} \\
qlSys_{(a\_liv,bas,cty,cz,sys)} &=
\big(qlPas^{LUH2}_{(cty,bas)} - qlPasRes_{(a\_liv,cty)}\big)
  \cdot grassUseRate_{(bas,cty)}
\nonumber\\[-0.3em]
  &\quad
\cdot
\frac{pasArea^{HERRERO}_{(a\_liv,bas,cty,cz,sys)}}
{\sum_{(a\_liv,cz,sys)} pasArea^{HERRERO}_{(a\_liv,bas,cty,cz,sys)}}
\end{align}

where \( livWt^{HERRERO} \) denotes production data
and \( pasArea^{HERRERO} \) represents pasture use data
from Herrero et al. (2018). The grass utilization rate is calculated from
estimated grass demand and maximum available supply, and capped in calibration
to avoid overuse.

The grazed and ungrazed pasture areas are then computed as:

\begin{align}
qlPasGrz_{(bas,cty)} &= \sum_{(a\_liv,cz,sys)} qlSys_{(a\_liv,bas,cty,cz,sys)} \\
qlPasUng_{(bas,cty)} &= qlPas^{LUH2}_{(cty,bas)} - qlPasGrz_{(bas,cty)}
\end{align}

Next, the feed requirements and concentrate feed share were estimated as follows:

\begin{align}
feedReq_{(a\_liv,bas,cty,cz,sys)} &=
\frac{feedQty^{HERRERO}_{(a\_liv,bas,cty,cz,sys)} + pasArea^{HERRERO}_{(a\_liv,bas,cty,cz,sys)}}
{livWt^{HERRERO}_{(a\_liv,bas,cty,cz,sys)}} \\
feedConcShr_{(a\_liv,bas,cty,cz,sys)} &=
\frac{feedQty^{HERRERO}_{(a\_liv,bas,cty,cz,sys)}}
{feedQty^{HERRERO}_{(a\_liv,bas,cty,cz,sys)} + pasArea^{HERRERO}_{(a\_liv,bas,cty,cz,sys)}}
\end{align}

where \( feedQty^{HERRERO} \) is the amount of concentrate feed from Herrero et al. (2018).

Using these values and the FBS feed demand data, 
the quantities of pasture and concentrate feed use were estimated. 
The amount of concentrate feed input was adjusted with a correction coefficient 
to ensure consistency with the FBS data. 
Consequently, the feed requirements and concentrate feed share were also adjusted.
If the estimated pasture use exceeded the available supply, 
it was replaced with the available amount.

\begin{multline}
qFeedSys^{temp}_{(a\_liv,c\_fed,bas,cty,cz,sys)} =
\frac{qsSys_{(a\_liv,bas,cty,cz,sys)}}
     {animalProdRate_{(a\_liv,cty)}} \\
     \cdot
feedReq_{(a\_liv,bas,cty,cz,sys)} \cdot
feedConcShr_{(a\_liv,bas,cty,cz,sys)} \\
\cdot
\frac{qdFed^{FAO}_{(c\_fed,cty)}}
     {\sum_{c\_fed} qdFed^{FAO}_{(c\_fed,cty)}}
\end{multline}

\begin{align}
feedScale_{(c\_fed,cty)} &=
\frac{qdFed^{FAO}_{(c\_fed,cty)}}
     {\displaystyle
      \sum_{(a\_liv,bas,cz,sys)}
      qFeedSys^{temp}_{(a\_liv,c\_fed,bas,cty,cz,sys)}}
\end{align}

\begin{multline}
qFeedSys_{(a\_liv,c\_fed,bas,cty,cz,sys)} =
qFeedSys^{temp}_{(a\_liv,c\_fed,bas,cty,cz,sys)} \cdot
feedScale_{(c\_fed,cty)}
\end{multline}

\begin{multline}
qGrassSys_{(a\_liv,bas,cty,cz,sys)} =
\frac{qsSys_{(a\_liv,bas,cty,cz,sys)}}
     {animalProdRate_{(a\_liv,cty)}} \\
     \cdot
feedReq_{(a\_liv,bas,cty,cz,sys)}
\cdot \bigl(1 - feedConcShr_{(a\_liv,bas,cty,cz,sys)}\bigr)
\end{multline}


If the estimated pasture demand exceeded the available supply, 
it was replaced with the available amount:

\begin{multline}
qGrassSys_{(a\_liv,bas,cty,cz,sys)} =
qlSys_{(a\_liv,bas,cty,cz,sys)} \cdot yGrass_{(bas,cty)} \\
\text{if }
qlSys_{(a\_liv,bas,cty,cz,sys)} \cdot yGrass_{(bas,cty)}
< qGrassSys_{(a\_liv,bas,cty,cz,sys)}
\end{multline}


where:
\begin{itemize}
\item \( qFeedSys^{temp} \): estimated feed demand by production system,
\item \( qdFed^{FAO} \): feed demand data from FAO,
\item \( feedScale \): adjustment coefficient for consistency with FAO feed demand,
\item \( qFeedSys \): adjusted feed demand consistent with FAO data,
\item \( qGrassSys \): pasture demand by production system.
\end{itemize}


\subsubsection{Adjustment of food trade}

Next, the adjustment of food trade data was conducted. 
In order to express trade through complementary conditions, 
the net import and net export quantities were derived as follows:

\begin{align}
qm_{(c,cty)} &= qm^{FAO}_{(c,cty)} - qe^{FAO}_{(c,cty)}
\quad (\text{if } qm^{FAO}_{(c,cty)} > qe^{FAO}_{(c,cty)}) \\
qe_{(c,cty)} &= qe^{FAO}_{(c,cty)} - qm^{FAO}_{(c,cty)}
\quad (\text{if } qe^{FAO}_{(c,cty)} > qm^{FAO}_{(c,cty)})
\end{align}

where \( qm^{FAO} \) denotes the import quantity data from FAO,
and \( qe^{FAO} \) denotes the export quantity data from FAO.

In the FBS dataset, global trade is not balanced; that is, total world exports and imports do not match. The discrepancy is retained as the signed accounting residual \(qRes\), rather than interpreted as a physical transport loss.

\begin{equation}
qRes_{c} = \sum_{cty} qe_{(c,cty)} - \sum_{cty} qm_{(c,cty)}
\end{equation}



\subsubsection{Estimation of land constraints}

Next, the estimation method for land areas unsuitable for agricultural use 
due to soil and topographical constraints is described.  
From the GAEZ dataset, area data for 57 agro-ecological zone (AEZ) classes, 
classified according to climate, soil, and topographical conditions, 
were obtained.  
Among these, land classes with strong constraints were selected 
and considered unsuitable for agricultural use.  
The area of such constrained land was calculated as follows:

\begin{equation}
qlCroCap_{(bas,cty)} =
qlTot^{LUH2}_{(bas,cty)} \cdot
\frac{\sum_{aezlim} qlGaez_{(aezlim,bas,cty)}}
{\sum_{aez} qlGaez_{(aez,bas,cty)}}
\end{equation}

where:
\begin{itemize}
\item \( qlTot^{LUH2} \): total land area from LUH2,
\item \( qlGaez \): area data by AEZ class from GAEZ,
\item \( aez \): the set of 57 AEZ classes,
\item \( aezlim \in aez \): AEZ classes with severe land-use constraints.
\end{itemize}

In addition, protected-area information was incorporated using the World Database on Protected Areas (WDPA)~\cite{iucnUNEPWCMCWorldDatabase2025}.  
The WDPA polygons were overlaid with basin-country units and classified into primary forest, secondary forest, other natural land, pasture, and rangeland. These areas form \(\overline{qlProt}_{(bas,cty,lu\_pa)}\), which is imposed as a lower bound on the corresponding land class; pasture and rangeland share the pasture constraint.

Urban land is based on the LUH2-consistent base-year area in the default model configuration.  
The base-year value is held constant in future years unless the optional SEDAC urban-projection extension is enabled.  
For this extension, the SEDAC 1-km urban fraction data~\cite{gaoGlobal1kmDownscaled2021,gaoDownscalingSSPconsistent2021} were aggregated to basin-country units and converted to annual SSP-specific series.  
When the extension is switched on, these SEDAC series are used to update urban land in scenario runs while preserving the LUH2-consistent base-year level.  
To maintain physical consistency of the land balance, urban area was bounded by zero and by available land after subtracting wat/ice.

\subsubsection{Estimation of forest carbon data}

Forest and other natural-land carbon stocks are derived from spatial outputs of AIM-PLUM and VISIT and aggregated to basin-country units~\cite{hasegawaGlobalLanduseAllocation2017,itoWaterUseEfficiencyTerrestrial2012}. Forest growth by stand age is used to calculate carbon sequestration from annual afforestation~\cite{hasegawaCarefulSelectionForest2024}.


\subsubsection{Adjustment of price data}

From the FAO dataset, producer prices, import prices, and export prices 
for each country and commodity were obtained. 
Since the price data contained many outliers and missing values, 
appropriate preprocessing was conducted.  

First, outlier values were treated as missing when the producer price 
was greater than ten times or less than one-tenth of the country-level median.

\begin{equation}
pp_{(c,cty)} \leftarrow \text{NA}
\quad \text{if }
\big( pp_{(c,cty)} > 10 \cdot \text{median}_{cty}(pp_{(c,cty)})
\ \cup \
pp_{(c,cty)} < 0.1 \cdot \text{median}_{cty}(pp_{(c,cty)}) \big)
\end{equation}

Next, missing values were complemented.  
A world average price was calculated from the available country-level producer prices, 
weighted by production quantities.  
A country-specific correction coefficient was then derived 
based on the deviation of each country's prices from the world average.  
This coefficient was used to impute the missing prices.  
The same procedure was applied for import and export prices.

\begin{align}
&pAvg_{c} =
\frac{\sum_{cty} pp_{(c,cty)} \cdot qs_{(c,cty)}}{\sum_{cty} qs_{(c,cty)}} \\
&pRelAvg_{cty} =
\frac{\sum_{c} pp_{(c,cty)}}{pAvg_{c}} \\
&pp_{(c,cty)} =
pAvg_{c} \cdot pRelAvg_{cty}
\end{align}

where \( pAvg \) denotes the global average producer price,
and \( pRelAvg \) denotes the country-specific adjustment coefficient.

\bigskip

Next, using the GTAP10 dataset, exogenous variables related to prices were estimated.  
The Producer Support Estimate (PSE) represents the monetary transfers 
from consumers and taxpayers to producers resulting from agricultural policies.  
In this study, the PSE is expressed as the proportionate impact of 
subsidies and taxes on producer prices.  
Similarly, the Consumer Support Estimate (CSE) represents 
the transfers from or to consumers, 
expressed as the proportionate impact of subsidies and taxes on consumer prices.

\begin{align}
pse_{(i,cty)} &=
\frac{
OSEP^{GTAP}_{(i,cty)} 
+ \sum_i ISEP^{GTAP}_{(i,ii,cty)} 
- \sum_{endw} FTRV^{GTAP}_{(i,endw,cty)} 
+ \sum_{endw} FBEP^{GTAP}_{(i,endw,cty)}
}{
VOA^{GTAP}_{(i,cty)}
} \\
cse_{(i,cty)} &=
\frac{VDPM^{GTAP}_{(i,cty)} - VDPA^{GTAP}_{(i,cty)}}{VDPM^{GTAP}_{(i,cty)}}
\end{align}

where:
\begin{itemize}
\item \( OSEP^{GTAP} \): subsidies/taxes on production,  
\item \( ISEP^{GTAP} \): subsidies/taxes on intermediate inputs,  
\item \( FTRV^{GTAP} \): taxes on factor inputs,  
\item \( FBEP^{GTAP} \): subsidies on factor inputs,  
\item \( VOA^{GTAP} \): production value,  
\item \( VDPM^{GTAP}, VDPA^{GTAP} \): household consumption at market and consumer prices, respectively.
\end{itemize}

\textit{Indices:}  
\( i \): production sector;  
\( ii \): intermediate input sector;  
\( endw \): primary factor of production;  
\( cty \): reporting country;  
\( ccty \): partner country;  

\bigskip

To link producer (farm-gate) prices with consumer (market) prices, 
the market margin ratio \( mmj \) was estimated.
Intermediate inputs and factor inputs corresponding to market margins 
were identified from the GTAP input–output tables, 
and their total was divided by the production value to obtain the ratio.  
Similarly, trade-related costs \( mmm \) and \( mme \) were estimated from GTAP data,
using the difference between import values at CIF prices and export values at FOB prices.

\begin{align}
mmj_{(i,cty)}
&=
\frac{
  \sum_{immj} VDFA^{GTAP}_{(i,immj,cty)}
  + \sum_{immj} VIFA^{GTAP}_{(i,immj,cty)}
  + \sum_{immj} EVFA^{GTAP}_{(i,immj,cty)}
}{
  \left(
  \begin{array}{@{}l@{}}
    \displaystyle
    \sum_{ii} VDFA^{GTAP}_{(i,ii,cty)}
    + \sum_{ii} VIFA^{GTAP}_{(i,ii,cty)}
    + \sum_{endw} EVFA^{GTAP}_{(i,endw,cty)} \\[0.2em]
    \displaystyle
    - \Bigl(
        \sum_{immj} VDFA^{GTAP}_{(i,immj,cty)}
      + \sum_{immj} VIFA^{GTAP}_{(i,immj,cty)}
      + \sum_{immj} EVFA^{GTAP}_{(i,immj,cty)}
      \Bigr)
  \end{array}
  \right)
}
\\[0.5em]
mmm_{(i,ccty)}
&=
\frac{
  \sum_{cty} VIWS^{GTAP}_{(i,cty,ccty)}
  - \sum_{cty} VXWD^{GTAP}_{(i,cty,ccty)}
}{
  \sum_{cty} VXWD^{GTAP}_{(i,cty,ccty)}
}
\\[0.5em]
mme_{(i,cty)}
&=
\frac{
  \sum_{ccty} VIWS^{GTAP}_{(i,cty,ccty)}
  - \sum_{ccty} VXWD^{GTAP}_{(i,cty,ccty)}
}{
  \sum_{ccty} VXWD^{GTAP}_{(i,cty,ccty)}
}
\end{align}


where:
\begin{itemize}
\item \( VDFA^{GTAP}, VIFA^{GTAP}, EVFA^{GTAP} \): intermediate and factor input values at consumer prices,  
\item \( VIWS^{GTAP}, VXWD^{GTAP} \): import (CIF) and export (FOB) values,  
\item \( immj \in (i \cup endw) \): goods and factors corresponding to market margins.
\end{itemize}

\bigskip

The import tariff (\( tm \)) and export tariff (\( te \)) were then calculated.
The import tariff rate was obtained as the ratio of import value at domestic prices 
to that at international prices, 
and the export tariff rate as the ratio of export value at international prices 
to that at domestic prices.

\begin{align}
&tm_{(i,ccty)} =
\frac{\sum_{cty} VIMS^{GTAP}_{(i,cty,ccty)}}{\sum_{cty} VIWS^{GTAP}_{(i,cty,ccty)}} \\
&te_{(i,cty)} =
\frac{\sum_{ccty} VXWD^{GTAP}_{(i,cty,ccty)}}{\sum_{ccty} VXMD^{GTAP}_{(i,cty,ccty)}}
\end{align}

where:
\begin{itemize}
\item \( VIMS^{GTAP} \): import value at domestic (market) prices,  
\item \( VXMD^{GTAP} \): export value at domestic (market) prices.
\end{itemize}

\bigskip

Finally, coefficients for decomposing producer prices by input factor 
were estimated.  
Each agricultural commodity is produced using land, feed, raw materials, 
and other production factors.  
Using the GTAP input–output tables, the cost share of each factor 
in the producer price was estimated.  
By multiplying the producer price by these shares, 
the base-year prices of each factor were obtained.

\begin{align}
landCostShr_{(i,cty)}
&=
\frac{
  \sum_{ilnd} VDFA^{GTAP}_{(i,ilnd,cty)}
  + \sum_{ilnd} VIFA^{GTAP}_{(i,ilnd,cty)}
  + \sum_{ilnd} EVFA^{GTAP}_{(i,ilnd,cty)}
}{
  \left(
  \begin{array}{@{}l@{}}
    \displaystyle
    \sum_{ii} VDFA^{GTAP}_{(i,ii,cty)}
    + \sum_{ii} VIFA^{GTAP}_{(i,ii,cty)}
    + \sum_{endw} EVFA^{GTAP}_{(i,endw,cty)} \\[0.2em]
    \displaystyle
    - \Bigl(
        \sum_{immj} VDFA^{GTAP}_{(i,immj,cty)}
      + \sum_{immj} VIFA^{GTAP}_{(i,immj,cty)}
      + \sum_{immj} EVFA^{GTAP}_{(i,immj,cty)}
      \Bigr)
  \end{array}
  \right)
}
\\[0.6em]
feedCostShr_{(i,cty)}
&=
\frac{
  \sum_{ifed} VDFA^{GTAP}_{(i,ifed,cty)}
  + \sum_{ifed} VIFA^{GTAP}_{(i,ifed,cty)}
  + \sum_{ifed} EVFA^{GTAP}_{(i,ifed,cty)}
}{
  \left(
  \begin{array}{@{}l@{}}
    \displaystyle
    \sum_{ii} VDFA^{GTAP}_{(i,ii,cty)}
    + \sum_{ii} VIFA^{GTAP}_{(i,ii,cty)}
    + \sum_{endw} EVFA^{GTAP}_{(i,endw,cty)} \\[0.2em]
    \displaystyle
    - \Bigl(
        \sum_{immj} VDFA^{GTAP}_{(i,immj,cty)}
      + \sum_{immj} VIFA^{GTAP}_{(i,immj,cty)}
      + \sum_{immj} EVFA^{GTAP}_{(i,immj,cty)}
      \Bigr)
  \end{array}
  \right)
}
\\[0.6em]
intCostShr_{(i,cty)}
&=
\frac{
  \sum_{iimd} VDFA^{GTAP}_{(i,iimd,cty)}
  + \sum_{iimd} VIFA^{GTAP}_{(i,iimd,cty)}
  + \sum_{iimd} EVFA^{GTAP}_{(i,iimd,cty)}
}{
  \left(
  \begin{array}{@{}l@{}}
    \displaystyle
    \sum_{ii} VDFA^{GTAP}_{(i,ii,cty)}
    + \sum_{ii} VIFA^{GTAP}_{(i,ii,cty)}
    + \sum_{endw} EVFA^{GTAP}_{(i,endw,cty)} \\[0.2em]
    \displaystyle
    - \Bigl(
        \sum_{immj} VDFA^{GTAP}_{(i,immj,cty)}
      + \sum_{immj} VIFA^{GTAP}_{(i,immj,cty)}
      + \sum_{immj} EVFA^{GTAP}_{(i,immj,cty)}
      \Bigr)
  \end{array}
  \right)
}
\\[0.6em]
1-ipt2_{(i,cty)}
&=
1 - \bigl(landCostShr_{(i,cty)} + feedCostShr_{(i,cty)} + intCostShr_{(i,cty)}\bigr)
\end{align}


where:
\begin{itemize}
\item \( landCostShr \): share of land cost in the producer price,
\item \( feedCostShr \): share of feed cost in the producer price,
\item \( intCostShr \): share of raw material cost in the producer price,
\item \( 1-ipt2 \): share of other production factors in the producer price,
\item \( ilnd, ifed, iimd \in (i \cup endw) \): goods and factors corresponding to land, feed, and raw materials,  
\item The denominator represents the total value of domestic and imported intermediate inputs 
      and factor inputs, excluding those corresponding to market margin sectors.
\end{itemize}
